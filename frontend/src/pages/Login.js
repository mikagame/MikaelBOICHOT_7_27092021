import React from 'react';
import Navigation from '../components/Navigation';

const Login = () => {
    return (
        <div>
             <Navigation />
            <h1>Login</h1>
        </div>
    );
};

export default Login;